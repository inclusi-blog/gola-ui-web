version: '3.7'
services:

  build-env:
    image: node:10-alpine
    volumes:
      - jenkins_home:/gola
    working_dir: /gola/gola-web-ui
volumes:
  jenkins_home:
    external: true
    name: jenkins_home
