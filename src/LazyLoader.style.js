import styled from 'styled-components';

const LazyLoaderContainer = styled.div`
  display: flex;
  align-items: center;
  justify-content: center;
  height: 100%;

  img {
    width: 70px;
    height: 70px;
  }
`;

export default LazyLoaderContainer;
